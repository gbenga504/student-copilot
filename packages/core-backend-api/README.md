# Core Backend API

NestJS API for the core application services.

## Development

```sh
npm install
npm run start:dev
```

The health endpoint is available at `GET /health` and returns `OK`.

## Quality checks

```sh
npm run check
npm run typecheck
npm test
```
